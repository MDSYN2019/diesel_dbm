name: fresh_segments
version: 1.0.0
config-version: 2

profile: fresh_segments

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fresh_segments:
    +materialized: view
    staging:
      +schema: analytics_staging
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table
